# Write a Python program to display a user entered name followed by Good Afternoon using the input() function.

name = input("Enter your name: ") # Get user input for name
print("Good Afternoon, " + name) # Display the message with the entered name

# or 
print(f"Good Afternoon, {name}") # Using f-string for formatting
