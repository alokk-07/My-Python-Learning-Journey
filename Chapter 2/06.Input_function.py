a=int(input("Enter number 1:"))  # The input function takes input from the user and returns it as a string. We can convert it to an integer using the int() function.
b=int(input("Enter number 2:"))  # The input function takes input from the user and returns it as a string. We can convert it to an integer using the int() function.

print("Number a is:", a)
print("Number b is:", b)
print("Sum of a and b is:", a+b) # This will concatenate the strings instead of adding them
print("Sum of a and b is:", int(a)+int(b)) # This will convert the strings to integers and then add them


