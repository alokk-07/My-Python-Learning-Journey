# Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}


# Answer: ❌ No, because lists cannot be stored in a set. 
# Therefore, the given set is invalid and its values cannot be changed.
#  This is a common Python exam question.

